fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
supabase>=2,<3
pydantic>=2,<3
python-dotenv>=1,<2
websockets>=14,<15
azure-identity>=1.15.0
pyyaml>=6,<7
# Linux Foundation Agent2Agent (A2A) Protocol — official Python SDK
# (https://github.com/a2aproject/a2a-python — donated by Google, now under LF
# Agentic AI Foundation). Pinned to 1.0.x — the v1.0 wire migration; the
# protocol surface and AgentCard schema changed significantly from 0.3.
a2a-sdk[http-server]>=1.0,<2
httpx>=0.27,<1
# Pydantic Logfire — AI observability (Pydantic AI native). No-op without
# LOGFIRE_TOKEN thanks to send_to_logfire='if-token-present'. The [fastapi]
# extra pulls in opentelemetry-instrumentation-fastapi for
# logfire.instrument_fastapi(app). Pinned to the 4.x line.
logfire[fastapi]>=4.0,<5
# Anthropic Model Context Protocol — official Python SDK
# (https://github.com/modelcontextprotocol/python-sdk). Pinned to >=1.12,<2
# so the FastMCP convenience API (mcp.server.fastmcp.FastMCP) and stable
# call_tool() / list_tools() surface used by apps/api/mcp/* remain available.
mcp>=1.12,<2
# Pulled by packages/llm (quorum_llm). Listed explicitly so apps/api builds
# even when quorum_llm is installed via path (Railway image installs both).
openai>=1.0.0
anthropic>=0.39.0
pydantic-ai-slim[openai,anthropic]>=1.94.0,<2.0
