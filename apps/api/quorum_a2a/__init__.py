"""Quorum's local A2A (Agent-to-Agent) integration layer.

This package is named ``quorum_a2a`` rather than ``a2a`` to avoid shadowing
the ``a2a-sdk`` top-level ``a2a`` namespace from PyPI (which we depend on
for the LF A2A 1.0 protocol types and FastAPI/Starlette router helpers).
"""
