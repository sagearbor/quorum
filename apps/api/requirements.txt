fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
supabase>=2,<3
pydantic>=2,<3
python-dotenv>=1,<2
websockets>=14,<15
azure-identity>=1.15.0
pyyaml>=6,<7
# Linux Foundation Agent2Agent (A2A) Protocol — official Python SDK
# (https://github.com/a2aproject/a2a-python — donated by Google, now under LF
# Agentic AI Foundation). Pinned to 1.0.x — the v1.0 wire migration; the
# protocol surface and AgentCard schema changed significantly from 0.3.
a2a-sdk[http-server]>=1.0,<2
httpx>=0.27,<1
